//append custom loader (appendLoader is in jsparty/loader.js)
appendLoader(function() {
	//Hide the drag and drop checkbox and its label
	jQuery('.CMSMain #TreeTools #sortitems').hide().next('label').hide();
});

